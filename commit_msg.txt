fix: admin API POST/PUT body handling + add auto virtual model

Admin API (POST/PUT):
- RouterConfig: add BodyHandler per-route for /api/* POST/PUT endpoints
  (not globally — relay /v1/chat/completions must NOT have BodyHandler)
- VirtualModelController: add null body guard + use getBody().toJsonObject()
- ModelCatalogController: same body handling fixes for create/update
- InstanceController: same fixes for create/update
- VendorController: same fixes for parseBody()
- ModelCatalogControllerTest: mock getBody() in mockBody()

Also:
- Add 'auto' virtual model entry (same models as flagship)
- META-INF/services/java.sql.Driver via AppendingTransformer in shade plugin
